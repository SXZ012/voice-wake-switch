# ============================================================
# 离线语音唤醒智能开关 - 依赖清单
# ============================================================

# Vosk 离线语音识别引擎
vosk

# 麦克风音频采集
pyaudio

# MQTT 消息协议客户端（用于物联网通信）
paho-mqtt

# 数值计算（音频信号处理用）
numpy

# 环境变量管理（可选，方便后续扩展）
python-dotenv

# ============================================================
# 注意：
# - RPi.GPIO 是树莓派上的 GPIO 控制库
#   在树莓派上运行需额外安装：pip install RPi.GPIO
#   在 Mac / Windows 开发时无需安装，程序会自动切换到模拟模式
# ============================================================
